{
  "name": "dink-filter",
  "version": "1.0.0",
  "description": "Middleware filter between Dink (RuneLite) and Discord webhooks",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": "18.x"
  }
}
